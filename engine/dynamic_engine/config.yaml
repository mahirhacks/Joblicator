# Dynamic engine — local Ollama LLM settings

ollama:
  base_url: http://127.0.0.1:11434
  model: qwen3.5:9b
  think: false

generation:
  temperature: 0.5
  context_window: 32768

applications:
  json: applications/local_applications.json

profile:
  json: settings/profile.json

batching:
  max_chars: 1200

loops:
  max_keywords_per_batch: 8
